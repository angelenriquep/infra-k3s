# Kustomization for ArgoCD Applications
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - api-gateway.yaml

# Common labels for all ArgoCD applications
commonLabels:
  managed-by: argocd
  component: gitops

# Common annotations
commonAnnotations:
  argocd.argoproj.io/managed: "true"
